// Create an object with methods (add,subtract).

let operations = {
  a : 50,
  b : 20
};
let add =  operations.a + operations.b;
let difference = operations.a - operations.b;
console.log(add);
console.log(difference);