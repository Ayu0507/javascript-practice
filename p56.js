// Check wheather a property exists in an object.

let fruitColor = {
  apple : 'red',
  banana : 'yellow',
  cherry : 'red',
  berry : 'Blue',
}
if (fruitColor.grapes !== undefined)
  console.log('Exists')
else
  console.log('Does not exists')